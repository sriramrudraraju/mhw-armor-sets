import { StyleRules } from "@material-ui/core/styles";

import theme from "../../assets/themes/theme";

const appStyles: StyleRules<"app"> = {
  app: {
    fontFamily: theme.typography.fontFamily
  }
};

export default appStyles;
