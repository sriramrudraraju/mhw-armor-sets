import { StyleRules } from "@material-ui/core/styles";

import theme from "../../assets/themes/theme";

const Styles: StyleRules<"tabs"> = {
  tabs: {
    backgroundColor: theme.palette.primary.light
  }
};

export default Styles;
