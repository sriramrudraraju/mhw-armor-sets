import { StyleRules } from "@material-ui/core/styles";

import theme from "../../../assets/themes/theme";

const styles: StyleRules<"rightDrawer"> = {
  rightDrawer: {
    backgroundColor: theme.palette.primary.main
  }
} as any;

export default styles;
